DEBUG=1
SECRET_KEY=your-secret-key-here
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1 [::1]
DATABASE_URL=postgres://billify:billify_local@db:5432/billify
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Storage
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_STORAGE_BUCKET_NAME=billify-local
AWS_S3_ENDPOINT_URL=http://minio:9000

# Redis
REDIS_URL=redis://redis:6379/0

# Ponto Integration (Get these from Ponto Developer Portal)
PONTO_CLIENT_ID=your_ponto_client_id
PONTO_CLIENT_SECRET=your_ponto_client_secret

# Ponto URLs (Use sandbox URLs for development)
PONTO_AUTH_URL=https://sandbox.ibanity.com/ponto-connect/auth
PONTO_TOKEN_URL=https://api.ibanity.com/ponto-connect/oauth2/token
PONTO_REDIRECT_URI=http://localhost:8000/api/ponto/callback
URL=https://api.ibanity.com/ponto-connect/oauth2/token
BASE_URL=https://api.ibanity.com/ponto-connect/

# Security (Generate these securely and share via secure channel)
PRIVATE_KEY_PASSWORD=your_private_key_password
KEY_ID=your_key_id
FERNET_KEY=your_fernet_key  # Generate using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"